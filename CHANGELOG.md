# Changelog

> English version: [CHANGELOG-EN.md](CHANGELOG-EN.md)

## [1.1.0] - 2026-09-02

### 変更

- **Manta 2.0.0 に対応**（内包 Manta を 2.0.0 に更新、要求範囲 `[2.0.0,3)`）。
  他の BelugaLab mod（TSU 1.1.0 / ASC 1.2.0）も同時に更新してください。Manta 1.x を内包するビルドは `[1.1.74,2)` を要求し、両方の必須範囲を 1 つの Manta で満たすことはできません。

### 追加

- **途中参加でも、どの音も「途中から」聞こえるようになりました**
  1 回再生の音も、サーバー参加・別ディメンションからの移動・範囲内への進入のいずれでも配信されます。
  開始からの経過時間（自分のダウンロードにかかった時間も含む）ぶん進んだ位置から鳴ります。
  最初から聞いている人は自分の初回ダウンロードのぶん遅れているため、途中参加者のほうがその分だけ先行することがあります。
  音が終わったあとに範囲内へ来た人には何も鳴らず、その音は片付けられます。
- **通常再生の ∞ ボタン**（再生・停止の右）。単体スロットの媒体を止めるまで繰り返します。
  再生中に切り替えると、いま鳴っている音に**その場で**効きます: OFF は各プレイヤーが今のパスを最後まで聞いて止まり、ON は endless として鳴り直します。
- スケジュールの ON/OFF トグルはスケジュール画面のボタン行に移りました（本画面のトグルは廃止）。
- 範囲ボードスロット・減衰・範囲表示・スケジュールボタンを 1 行に整理。

### 修正

- 単体スロットの ∞ 再生が 10 分で止まっていた問題（安全網タイムアウトに到達していた）。
- 空スロットへのレッドストーンパルスが、走行中のスケジュールの ∞ エントリを黙って解除していた問題。
- ∞ 再生中に媒体を抜いても鳴り続けていた問題。
- 範囲ボードのツールチップとアップロード失敗メッセージが英語固定だった問題。

### 内部

- ネットワーク版 1.5（`client_play_audio` に開始オフセットと同期フラグ、`client_set_loop` / `catchup_report` 追加）。旧 client は接続時に拒否されます。
- サーバーログ `SAS-CatchUp` に、各クライアントが実際に適用した開始位置が記録されます。

## [1.0.6] - 2026-08-29

### 追加

- **無限ループ再生 (∞)**
  スケジュールの再生回数を 10 の 1 つ先まで回すと **∞** になり、止めるまで鳴り続けます。
  蛍光灯のハム・換気・機械音のような環境音を、サーバーが動いている間ずっと流すための機能です。
  ループはクライアント側で行われるため、**繰り返しても通信量は増えません**（音声の転送は 1 人につき 1 回だけ）。
  ワールドを保存して再起動しても、チャンクが読み込み直されても、ループは自動的に復帰します。
  ∞ を設定したエントリはその装置が最後に鳴らすものになり、後続のエントリには進みません。
- **後から来たプレイヤーにも ∞ の音が届くようになりました**
  サーバーに参加した場合、別のディメンションから移動してきた場合、
  単に再生範囲へ歩いて入ってきた場合のいずれでも、**その音が聞こえる位置にいれば**再生されます。
  範囲外のプレイヤーには送られないので、聞こえない音のために通信量を使うことはありません。

  （1.0.6 時点では ∞ の音だけが対象でした。1.1.0 で 1 回再生にも広がり、途中から鳴らすようになりました — 下記）
- **公開 API に `SasApi.playAudio(..., boolean loop)` を追加**
  既存の 5 引数版はそのまま使えます (`loop = false` 相当)。

### 修正

- **再生中に参加したプレイヤーに音が流れなかった問題を修正**
  音声は再生が始まった瞬間にオンラインだったプレイヤーにしか送られておらず、
  後から参加した人・別ディメンションから来た人には何も届いていませんでした。
- **プレイリストが無人のサーバーで永久に停止する問題を修正**
  次のエントリへ進む合図がクライアントからの再生終了報告だけだったため、
  全員が切断すると報告が届かず、シーケンスがその場で止まったままになっていました。
  ∞ の再生はこの往復自体を必要としません。

### 内部

- クライアントの音量計算とサーバーの範囲判定を `SpatialGain` に一本化しました。
  2 つの実装がずれると「聞こえないのに転送する」「範囲内なのに無音」のどちらかが起きますが、
  どちらも例外にならないため、実装を 1 つにして食い違いを構造的に不可能にしています。
- 再生セッションが「今どこで何が鳴っていて、誰にまだ送っていないか」を保持するようになりました。
- 検査を追加 (`SpatialGainTest` / `PlaybackLoopTest` / `PlaybackSessionRegistryTest`)。
  変異制御は `scripts/playback.mutation.py`。

## [1.0.5] - 2026-08-01

### 重要な変更

- **全画面を Manta UI へ移行**
  再生装置・記憶装置の画面を Manta UI ランタイム上の BelugaExperience デザインワークフローで再構築。旧 GUI テクスチャは撤去しました。
- **Manta を jar に内包 (jar-in-jar)**
  Manta 1.1.12 を同梱し、必須依存として宣言。ユーザーが Manta を別途インストールする必要はありません。

### 追加

- **♪ スケジュール (プレイリスト)**
  再生装置から複数の記録媒体を順番に再生できるエディタ。最大 6 エントリ、各エントリの再生回数は 1〜10 回。インベントリから行のスロットへドラッグ、または Shift クリックで最初の空き行へ割り当て。▲ / ▼ で並べ替え、行の ▶ で試聴、✕ で削除 (媒体は返却)。**▶ Play All** で通し再生し、再生中の行は枠線で示されて進行に合わせて移動します。再生回数はホバーしてホイールで変更します。
- **ゲーム内 wiki**
  各画面の **📖** ボタンでその画面の解説ページを開きます。ヒント ON のときは要素にホバーして **F1** を押すと、その機能のページへ直接飛べます。英語 / 日本語の 5 ページを収録。
- **ヒント表示**
  各画面右上のトグル。ON にすると、ホバーした要素の説明が画面左下に表示されます。
- **ジャケット (カバーアート) 表示**
  音声ファイルに埋め込まれたアートワークを記憶装置・再生装置の画面に表示します。JPEG のジャケットも Manta の画像デコーダ経由で表示されます。
- **アクセスモード (公開 / 非公開)**
  デバイスを最初に開いたプレイヤーが所有者になります。顔アイコンで公開 (緑) / 非公開 (赤) を切り替え、非公開の間は所有者以外は開けません。
- **範囲指定ボードの HUD**
  手に持つと画面下部に HUD が出ます。**Alt + ホイール**でモード切替、**Ctrl または Shift + ホイール**で現在モードの値を調整 (通常範囲指定モード以外)。ホイールには 180ms のクールダウンがあり、一度のフリックで連続変化しません。
- **書き込みエラーの通知**
  記憶装置での書き込み失敗が画面上に表示されるようになりました。

### Bug Fixes

- **1.0.3 以前のワールド資産が認識されない問題を修正**
  1.0.4 の改名では namespace だけが変わったため、チャンク内のブロック・インベントリ内のアイテム・ブロックエンティティ・メニュー・音声 ID を保持するデータコンポーネントが、いずれも存在しない Mod を指したままになっていました。旧 ID (`stationsoundsystem`) から現行 ID へのレジストリエイリアスを追加し、`AudioStorage` は旧ディレクトリ (`<world>/stationsoundsystem_audio/`) もフォールバックとして参照します。**1.0.3 以前のワールドの記録媒体・再生装置・範囲指定ボードがそのまま使えます** (1.0.4 の「新規ワールドへ移行が必要」という注意は解消されました)。

### 改善

- 再生セッションのレジストリとスケジューラを追加し、再生の開始 / 停止 / 完了通知を一元化。
- 再生のタイムアウト処理を追加。
- プレイリスト操作パケットの入力検証を強化。
- 音声ストレージ・ネットワーク・音声処理まわりを全体的に堅牢化。
- 操作系グリフを Manta のアイコンへ置き換え。
- ユニットテストを追加 (52 件)。

## [1.0.4] - 2026-05-05

### 重要な変更 (Mod のリネーム / ID 変更)

- **Mod 名を「Station Sound System」から「Spatial Audio System」に変更**
  鉄道駅専用ではなく汎用的な空間音響再生 Mod として位置づけを明確化。
  - Mod ID: `stationsoundsystem` → `spatialaudiosystem`
  - 表示名: `Station Sound System` → `Spatial Audio System`
  - Java パッケージ root: `com.example.stationsoundsystem` → `com.spatialaudiosystem`
  - メインクラス: `StationSoundSystem` → `SpatialAudioSystem`
  - リソース ID (`assets/`、`data/`)・GitHub リポジトリ名も同名に変更
  - **既存ワールドから 1.0.3 以前の記録媒体・再生装置・範囲指定ボードを引き継ぐと認識されないので、新規ワールドへ移行するか、各アイテム / ブロックを置き直す必要があります**

### 公開 API の追加 (他 Mod 連携用)

- **新パッケージ `belugalab.sas.api`** を追加。バージョン間で安定した API 表面として維持されます (内部パッケージ `com.spatialaudiosystem.*` はリパッケージされるため、addon mod は必ずこちらを参照してください)。
- **`belugalab.sas.api.SasApi`** — facade クラス
  - `isInstalled` — SAS が読み込まれているか
  - `isRecordingMedium`, `isRangeBoard` — アイテム種別判定
  - `hasAudio`, `getAudioFileName`, `getAudioFormat`, `getAudioId` — 記録媒体メタデータ
  - `hasRange`, `getRangePos1`, `getRangePos2`, `getAttenuationRanges` — 範囲指定ボードのメタデータ
  - `loadAudio`, `loadAudioFromMedium` — サーバー側の音声バイナリ読み込み
  - `playAudio` — サーバー側からブロードキャスト再生
  - `stopAudio` — 指定位置の再生停止
- **`belugalab.sas.api.PlaybackEndedEvent`** (NeoForge event)
  - 再生完了時に発火。連鎖再生 / シーケンシャル再生を実装する mod が polling 不要で連結できる
  - `PlaybackControlPayload` の停止通知ハンドラからも発火するため、`PlaybackDeviceBlockEntity` を介さない `playAudio` 呼び出し (= addon mod 側で動的に再生する場合) もカバー

### Bug Fixes

- **ワールドを抜けた後にタイトル画面で音声が鳴り続ける問題を修正**
  新規 `ClientLifecycleHandler` で `ClientPlayerNetworkEvent.LoggingOut` と `LevelEvent.Unload` を購読し、`AudioManager.stopAll()` を呼ぶように変更。送受信中の `ClientAudioChunkPayload` チャンクセッションも一括クリアするため、再ログイン後に旧セッションの残骸が混ざる問題も併せて解消。

## [1.0.3] - 2026-03-23

### 重要な変更

- **音声データのチャンク分割送信を実装**
  アップロード（Client→Server）・再生配信（Server→Client）の両方で音声データを500KB単位に分割して送信するよう変更。
  NeoForgeのパケットサイズ上限（約1MB）を超える音声ファイルでもサーバーからキックされなくなった。
  最大ファイルサイズを10MBに拡大。

### 改善

- **チャンク転送セッションのタイムアウト追加**
  アップロード・ダウンロードの未完了セッションは30秒で自動破棄。切断時のメモリリークを防止。

- **ClientNotifyPayload をoptional化**
  サーバーとクライアントのModバージョンが異なる場合でも接続拒否されなくなった。

- **lite inventory構築を自動化**
  `getUpdateTag()` のコンポーネントコピーを手動リストから `copy() + remove(AUDIO_DATA)` に変更。
  新コンポーネント追加時の漏れを防止。

- **範囲指定ボードの通知メッセージをHUDに移動**
  座標設定・クリア時のメッセージがアクションバーではなくアイテム名の下に表示されるよう変更。
  パネルとの重なりを解消。

### Bug Fixes

- **大容量音声ファイルの書き込みでキックされる問題を修正**
  単一パケットのサイズ上限を超える音声ファイルを送信しようとするとサーバーからキックされていた問題を修正。

- **大容量音声ファイルの再生で全プレイヤーがキックされる問題を修正**
  サーバーから全クライアントへの再生配信でも同様のパケットサイズ超過が発生していた問題を修正。

## [1.0.2] - 2026-03-22

### 重要な変更

- **音声データの保存方式をファイルベースに変更**
  音声データをItemStackのDataComponentから分離し、ワールドデータとしてサーバー側のファイルに保存するよう変更。
  `<world>/stationsoundsystem_audio/<uuid>.audio` に個別ファイルとして保存。
  ItemStackにはUUID参照（`AUDIO_ID`）のみ保持し、ネットワーク同期やクリエイティブモードによるデータ破損を根本的に解決。
  旧バージョンの記録媒体は初回再生時に自動でマイグレーションされる。

### 改善

- **Modアイコンを追加**
  Mod一覧に再生装置のアイコンが表示されるようになった。

- **範囲指定ボードのパネル文字を日本語化**
  モード名を「通常範囲指定」「減衰率設定」「下向き設定」に変更。方向名・説明文も日本語化。
  長いモード名がパネルからはみ出さないようスケーリング処理を追加。

- **範囲指定ボードの遠距離選択を追加**
  手の届かない位置のブロックも64ブロック先まで右クリックで選択可能に。

- **範囲指定ボードの追従アニメーションを追加**
  Pos1のみ設定時、Pos2が視線先になめらかに追従するプレビュー表示を追加。
  両ポイント未設定時も視線先に1ブロックのプレビューボックスを表示。

### Bug Fixes

- **バージョンアップ時に音声データが破損する問題を根本修正**
  音声データをファイルベースストレージに移行し、ItemStackのネットワーク同期を経由しなくなったため、
  クリエイティブモードやコンテナ操作による破損が発生しなくなった。

- **ワールド参加時に範囲外で音が鳴る問題を修正**
  レンダースレッドからフレームごとにゲイン更新を行うよう改善。

## [1.0.1] - 2026-03-12

### Bug Fixes

- **減衰モードが機能しない不具合を修正**
  バックグラウンドの音声スレッドから `ClientLevel` のプレイヤーリストに非スレッドセーフなアクセスを行っていたため、`ConcurrentModificationException` が内部でサイレントキャッチされ、ゲインが常に 1.0（最大音量）のままになっていた問題を修正。
  プレイヤー座標をメインスレッドからフレームごとにキャッシュし、音声スレッドはそのキャッシュを参照するよう変更。

- **減衰ゲインの更新タイミングを改善（オレンジ枠から出た瞬間に一瞬フルボリュームになる問題を修正）**
  以前は音声スレッドが約 260ms ごとにゲインを更新していたため、プレイヤーが範囲外に出た直後も一瞬フルボリュームで聞こえていた。
  レンダースレッド（約 60fps）でゲイン更新を行うよう変更し、応答速度を大幅に改善。

- **他のプレイヤーが再生した音声で減衰モードが機能しない問題を修正**
  上記のゲイン更新タイミング改善により、自分以外のプレイヤーが書き込んだ記録媒体を再生した場合でも、正しく減衰が適用されるようになった。

- **レッドストーン入力で音声が再生されない不具合を修正**
  マルチプレイヤー対応のリファクタリング時に `startPlayback()` のパケット送信処理が欠落していた問題を修正。レッドストーンの立ち上がりエッジで全クライアントに再生パケットを送信するよう復元。

- **記録媒体の音声データが破損する問題を修正**
  クリエイティブモードのアイテムスロット同期パケットがクライアント側の仮データ（プレースホルダー）でサーバー側の実データを上書きしてしまう問題を修正。
  再生時に不正なデータを検出した場合は再生をスキップするバリデーションを追加。

- **ブロック破壊時に他クライアントの音声が停止しない問題を修正**
  再生装置が破壊された際、`ClientStopAudioPayload` を全プレイヤーに送信するよう修正。
